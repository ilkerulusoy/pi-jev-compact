{
  "name": "pi-jev-compact",
  "version": "0.0.0",
  "description": "Selective, verbatim context compaction for Pi using TypeSafe's Jev model. Design stage; not implemented.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "keywords": [
    "pi-package",
    "pi-extension",
    "typesafe",
    "jev",
    "compaction"
  ],
  "engines": {
    "node": ">=22.19.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.85.1 <1"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": {
      "optional": true
    }
  },
  "scripts": {
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.13",
    "typescript": "^5.7.2"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "main": "index.ts"
}
